{
  "name": "quality-engineering",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Quality engineering skills for Claude Code",
  "scripts": {
    "lint": "biome check .",
    "lint:fix": "biome check --write .",
    "format": "biome format --write .",
    "test": "node --experimental-strip-types --test skills/*/pairwise.spec.ts skills/*/test-fixtures.spec.ts skills/*/breaking-change.spec.ts skills/*/resilience.spec.ts"
  },
  "devDependencies": {
    "@biomejs/biome": "^1.9.4"
  }
}
